import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    width: "90%",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: 16,
    paddingVertical: 16,
  },
});
